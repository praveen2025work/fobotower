"""The Helix console's view of the orchestrator.

Composes what the Helix screens render (recs, adjustments, analysis, MCP
calls, activity, chat) from the database and the LangGraph checkpoints. It
holds no data of its own: every figure here is read, never declared.
"""
